# Local-only example. Keep commented to avoid affecting cloud builds.
# To use local backend during vite dev, set this in your shell instead of committing:
#   $env:VITE_API_URL="http://localhost:8080/api"   (PowerShell)
# VITE_API_URL=http://localhost:8080/api

# You can also opt into OAuth in dev via:
# VITE_AUTH_PROVIDER=oauth
